<%= form_with model: artist do |form| %>
  <div class="form-group">
    <%= form.label :name, class: "form-label" %>
    <%= form.text_field :name, placeholder: "Enter the artist name", class: "form-input" %>
  </div>
  
  <div class="form-actions">
    <%= form.submit%>
  </div>
<% end %>
